---
const currentTime = new Date();
const currentHour = currentTime.getHours();

let greeting: string = "";

if (currentHour < 12) {
  greeting = "Buenos días";
}
if (currentHour < 18) {
  greeting = "Buenas tardes";
} else {
  greeting = "Buenas noches";
}
---

<h1 class="text-2xl py-6 px-4">{greeting}</h1>
